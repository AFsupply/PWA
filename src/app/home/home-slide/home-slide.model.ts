export class HomeSlide {

    public title: string;
    public content: string;
    public iconNames: string[];

    constructor(title: string, content: string, iconNames: string[]) {
        this.title = title;
        this.content = content;
        this.iconNames = iconNames;
    }
}
